TOOL=mariabackup
PREFIX=/usr/local/lib/mysql_backup
PLUGIN=scp
LOG=3

DATADIR=/var/mysql
HISTORY_FILE=backup_history.txt
DATABASE_CONNECT="-h 127.0.0.1"
DATABASE_USER="${DB_USERNAME}"
DATABASE_PASSWORD="${DB_PASSWORD}"

SFTP_SERVER=""
SFTP_ROOT_DIRECTORY="${SFTP_ROOT}"
SFTP_USER="${SFTP_USER}"
SFTP_PASSWORD="${SFTP_PASSWORD}"
SFTP_KEY="/root/.ssh/id_rsa"
